# טיוטה לעיון — סעיף פרטיות לשילוב ב-/privacy: «חיבור חשבון Gmail»

> **מסמך פנימי, טיוטה בלבד — טרם פורסם באתר.** נועד לבדיקה קפדנית מול הקוד ומול חבילת ההגשה ל-Google לפני קבלת החלטה אם וכיצד לעדכן את מדיניות הפרטיות הציבורית. כל טענה טכנית בטיוטה מגובה בקוד; ההתחייבויות התפעוליות מסומנות בטבלת המקורות בסוף (למטרתנו בלבד — אינה חלק מהנוסח המשפטי).

---

## חיבור חשבון Gmail

*(סעיף זה מיועד לשילוב בתוך מדיניות הפרטיות של Dubiz בכתובת /privacy, תחת כותרת ייעודית «חיבור חשבון Gmail».)*

**היקף:** סעיף זה מהווה חלק ממדיניות הפרטיות של Dubiz וחל **אך ורק** על המידע שאנו מקבלים מחשבון ה-Gmail שלך כאשר אתה בוחר לחבר אותו לשירות. בכל הנוגע למידע המתקבל מממשקי Google, יגברו הוראות סעיף זה.

### 1. איזה מידע מ-Gmail אנו ניגשים אליו

חיבור חשבון ה-Gmail הוא **וולונטרי** ומופעל אך ורק ביוזמתך, ונועד למטרה אחת: לאפשר לך לייבא למערכת מסמכים פיננסיים (כגון חשבוניות וקבלות) שהגיעו לתיבתך כקבצים מצורפים.

בעת חיבור החשבון, השירות פועל כך:
- בשלב האיתור, המערכת מאחזרת מ-Google את **תוכן ההודעות** התואמות לקריטריון איתור מצומצם — הודעות הכוללות קבצים מצורפים מתקופה אחרונה (למשל, מהחודש האחרון) — **לצורך אחד בלבד: לאתר בתוכן את הקבצים המצורפים**.
- מתוך הודעות אלו מוצגים לך ונשמרים אצלנו **אך ורק נתוני-העל של הקבצים המצורפים** (שם הקובץ, סוגו, גודלו ומזהים פנימיים). **גוף ההודעה אינו נשמר, אינו מוצג לך כחלק מהשירות, ואינו משמש לניתוח כללי של תיבת הדואר שלך.**
- רק לאחר שתבחר קובץ מסוים לייבוא, המערכת מורידה את אותו קובץ בלבד ומעבדת אותו.

הגישה שלנו לחשבונך היא **גישת קריאה בלבד**. איננו שולחים, עורכים, מוחקים, מתייגים או משנים בכל דרך אחרת הודעות, תוויות או הגדרות בתיבת הדואר שלך.

### 2. היקף ההרשאה (Gmail API Scope) ומטרתה

לצורך התכונה הזו אנו מבקשים את הרשאת Google המצומצמת ביותר המאפשרת קריאת תוכן קבצים מצורפים: `gmail.readonly` (בנוסף להרשאות הזיהוי הבסיסיות `openid`, `email`, `profile` לצורך זיהוי החשבון המחובר בלבד).

הרשאת `gmail.readonly` נדרשת משום שהורדת קובץ מצורף ואיתור מבנה ההודעה שמכיל אותו אפשריים רק דרך הרשאת קריאת תוכן. איננו מבקשים ואיננו משתמשים בהרשאות שליחה, עריכה, מחיקה או ניהול תוויות.

### 3. עיבוד OCR באמצעות Google Cloud Vision

לצורך חילוץ טקסט מהמסמך שבחרת לייבא, הקובץ נשלח לשירות **Google Cloud Vision** של Google — **אך ורק לצורך עיבוד OCR (המרת תמונה/מסמך לטקסט)** ולא לכל מטרה אחרת. זהו הצד השלישי היחיד שמקבל את תוכן המסמך שלך.

### 4. חילוץ נתונים פיננסיים — עיבוד מקומי

לאחר שלב ה-OCR, חילוץ שדות המידע הפיננסי (כגון סכום, שם ספק, תאריך וקטגוריה) מתבצע **באופן מקומי, על גבי התשתית שלנו**, באמצעות מנגנון עיבוד קבוע. שלב זה **אינו** כרוך בהעברת המידע לשירות בינה מלאכותית, למודל שפה או לצד שלישי כלשהו.

כעניין של מדיניות, **איננו משתמשים במידע המתקבל מחשבון ה-Gmail שלך כדי לאמן, לכוונן (fine-tuning) או לפתח מודלים כלליים או מערכות בינה מלאכותית / למידת מכונה.**

### 5. אחסון והצפנת אסימוני OAuth

כאשר אתה מחבר את חשבונך, Google מנפיקה לנו אסימוני הרשאה (Access ו-Refresh) המאפשרים לשירות לגשת בקריאה בלבד. אסימונים אלה נשמרים אצלנו **מוצפנים במנוחה, בתקן הצפנה מקובל (AES-256-GCM)**. איננו שומרים את סיסמת חשבון Google שלך; ההתחברות מתבצעת כולה מול Google.

### 6. ניתוק וביטול ההרשאה

תוכל לנתק את חשבון ה-Gmail מהשירות בכל עת. בעת ניתוק, המערכת:
- **מוחקת אצלנו את האסימונים המאוחסנים**, כך שלא ניתן עוד לעשות בהם שימוש; וכן
- **מנסה לבטל את ההרשאה מול Google** באמצעות מנגנון הביטול הרשמי של Google, על בסיס מאמץ סביר.

ניסיון הביטול מול Google נעשה כמיטב היכולת ואינו מובטח; אולם מחיקת האסימונים המקומית מתבצעת בכל מקרה, כך שהגישה שלנו לחשבונך נפסקת אצלנו ללא תלות בהצלחת הביטול מול Google. בנוסף, באפשרותך לבטל את ההרשאה בכל עת גם ישירות מהגדרות חשבון Google שלך (`myaccount.google.com/permissions`).

### 7. עמידה במדיניות Google API Services User Data ו-Limited Use

השימוש שלנו במידע המתקבל מממשקי Google, וכל העברה שלו, כפופים ל-**Google API Services User Data Policy**, לרבות דרישות ה-**Limited Use**. בהתאם לכך:
- המידע משמש **אך ורק** למתן התכונה הפונקציונלית שביקשת (ייבוא ועיבוד המסמכים שלך);
- המידע **אינו** משמש לפרסום או לפרסום ממוקד;
- איננו **מוכרים** את המידע;
- איננו **מעבירים** את המידע לצדדים שלישיים, למעט לספקי שירות הדרושים למתן התכונה (Google Cloud Vision לצורך OCR, כאמור בסעיף 3);
- **גישה אנושית מוגבלת:** עיבוד המידע הוא אוטומטי, ועובדי Dubiz אינם ניגשים לתוכן המסמכים שלך במהלך הרגיל של מתן השירות. גישה כזו עשויה להתרחש רק במקרים מוגדרים ומצומצמים: כאשר הדבר נדרש כדי לקיים חובה שבדין; לצורך אבטחת מידע ומניעת הונאה או שימוש לרעה; או במסגרת תמיכה טכנית שביקשת, ובהסכמתך.

> *Dubiz's use and transfer to any other app of information received from Google APIs will adhere to the [Google API Services User Data Policy](https://developers.google.com/terms/api-services-user-data-policy), including the Limited Use requirements.*

### 8. שמירת מידע וזכויות המשתמש

- **קבצים זמניים:** הקובץ הזמני הנוצר לצורך עיבוד ה-OCR נמחק בתום עיבוד הבקשה (בין אם הצליחה ובין אם נכשלה) ואינו נשמר מעבר לכך.
- **אסימונים:** אסימוני ההרשאה נמחקים בעת ניתוק החשבון, כמפורט בסעיף 6.
- **מסמכים שיובאו:** מסמך שבחרת לייבא והשדות שחולצו ממנו נשמרים כרשומות שלך במערכת, כל עוד נדרש לצורך מתן השירות או בהתאם לחובות שמירה שבדין.
- **זכויותיך:** באפשרותך לבקש גישה, תיקון או מחיקה של המידע האישי שלך, בכפוף לחובות שמירה חוקיות. ניתוק חשבון ה-Gmail אינו מוחק אוטומטית מסמכים שכבר ייבאת; לבקשת מחיקה של רשומות אלו, פנה אלינו.

### יצירת קשר

בשאלות בנוגע לנספח זה או לטיפול במידע שלך: **support@promaxgroup.co.il**.

---

## טבלת מקורות פנימית (לא לפרסום — לאימות שלנו)

לכל סעיף: מקור הטענה — ✅ עובדת-קוד (עם קובץ) או 📋 מדיניות/התחייבות עסקית.

| סעיף | טענה | מקור |
|---|---|---|
| 1 | קריאה בלבד; אין שליחה/עריכה/מחיקה/תיוג | ✅ `oauth-url.service.ts` (scopes); אין קריאות write בקוד |
| 1 | discovery: "תקופה אחרונה (למשל חודש)" — מאחזר full message לאיתור, שומר metadata בלבד | ✅ `gmail-discovery.service.ts` (`discoverGmailAttachments`, `collectAttachments`, `format=full`). הערה: `newer_than:30d` הוא **ברירת-מחדל** (`params.query || …`) — לכן נוסח רוכך ל"תקופה אחרונה" |
| 1 | גוף ההודעה אינו נשמר/מוצג/מנותח | ✅ discovery מחזיר `GmailAttachmentMetadata[]` בלבד; 📋 "לא לניתוח כללי" = התחייבות תפעולית |
| 1 | רק הקובץ שנבחר יורד | ✅ `gmail-attachment-fetch.service.ts` + `import/route.ts` (POST per msg+att) |
| 2 | scope = `gmail.readonly` (+openid/email/profile), הכי מצומצם | ✅ `oauth-url.service.ts` (`GMAIL_OAUTH_SCOPES`) |
| 3 | OCR ב-Google Cloud Vision בלבד | ✅ `google-vision-ocr.service.ts` (`runGoogleVisionOCR`) |
| 4 | חילוץ מקומי במנגנון קבוע, ללא צד ג'/LLM | ✅ `unified-extraction-engine.service.ts` (סטטי, אין קריאות חיצוניות); OPENAI לא בנתיב זה |
| 4 | אין שימוש בנתונים לאימון/כוונון/פיתוח מודלים AI/ML | 📋 התחייבות מדיניות, **עקבית עם הקוד**: מנוע סטטי; `correction-ledger.service.ts` שומר **hash של OCR (לא תוכן)** append-only ל-audit; אין לולאת train/fine-tune (grep ריק) |
| 5 | אסימונים מוצפנים AES-256-GCM at rest | ✅ `token-crypto.placeholder.ts`; `callback/route.ts` (`encryptToken`→`OAuthToken`) |
| 6 | ניתוק מוחק אסימון מקומי | ✅ `gmail-connection.service.ts` (`disconnectGmailConnection`, `oAuthToken.deleteMany`) |
| 6 | ביטול best-effort מול Google (לא מובטח) | ✅ `gmail-token-revoke.service.ts` (`REVOKE_ENDPOINT`, "never throws") |
| 6 | ניתן לבטל גם דרך Google Account | ✅ עובדה על Google; 📋 הכוונה למשתמש |
| 7 | לא לפרסום / לא נמכר / לא מועבר מלבד processors | 📋 התחייבות תפעולית (Limited Use); העברה ל-Vision מגובה ב-✅ סעיף 3 |
| 7 | גישה אנושית מוגבלת + חריגים ספציפיים (דין/אבטחה/תמיכה-בהסכמה) | 📋 התחייבות תפעולית (לא נאכף ע"י קוד); חריגים ספציפיים ל-Dubiz, לא רשימה גנרית |
| 8 | קובץ זמני נמחק בתום הבקשה (finally) | ✅ `import/route.ts` (finally→cleanup); `temp-ocr-file.service.ts` (`unlink`) |
| 8 | אסימון נמחק בניתוק | ✅ סעיף 6 |
| 8 | שמירת מסמכים "כל עוד נדרש"; זכות מחיקה | 📋 התחייבות/דין (עקבי עם מדיניות קיימת) |

**עקביות מול חבילת ההגשה:** כל 8 הסעיפים תואמים ל-Sections 1–2 ול-Evidence Matrix בחבילת ההגשה (`google-gmail-verification-submission-package-v1.md`). אין בטיוטה זו אף התחייבות שאינה מגובה בקוד או שאינה מסומנת כמדיניות, ואין הרחבת הרשאות/תהליכים מעבר לקיים.
