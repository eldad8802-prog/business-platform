# Dubiz — Homepage Final Candidate · Copy Record (V2.1 base)

> **סוג המסמך:** רשומת קופי מחייבת ל-**Final Candidate** (`/home-candidate`). נכתב **לפני** הקוד, לפי חוק *document first → code second*.
> **בסיס מוצר:** Homepage Prototype **V2.1** (`app/(corporate)/home-prototype/page.tsx`, #342) — החלטת מוצר נעולה.
> **כפוף ל (מנצחים בכל סתירה):** `dubiz-homepage-pre-copy-gate-v1.md` · `dubiz-homepage-copy-v1.md` · `dubiz-homepage-structural-wireframe-v2.md` · `dubiz-homepage-visual-design-spec-v1.md`.
> **תאריך:** 2026-09-20 · **base:** `origin/main` @ `b7c1a840`.

---

## 0. למה המסמך הזה קיים

V2.1 הוא **אב-טיפוס**. כמעט כל מחרוזת בו מסומנת בקוד `/* PLACEHOLDER copy */` — כלומר **מעולם לא עברה copy gate**. Homepage v1 (`/home`) לעומת זאת מתעתק את `copy-v1.md` verbatim ועבר את השער במלואו.

לכן ה-Final Candidate אינו "V2.1 עם תיקונים ויזואליים". הוא **V2.1 עם קופי שעבר שער**:

| מקור המחרוזת | משמעות |
|---|---|
| **`RATIFIED`** | verbatim מ-`copy-v1.md` או מקוד מאושר קיים. אין צורך באישור נוסף. |
| **`PROMOTED`** | נוסח V2.1 שנבדק מול `pre-copy-gate-v1` §8/§13 ונמצא תקין — מקודם מ-PLACEHOLDER למחייב. |
| **`REWRITTEN`** | נוסח V2.1 ש**הפר** את השער. הוחלף. הסיבה מתועדת. |
| **`NEW`** | מחרוזת חדשה שנדרשה ע"י החלטת בעלים (CHANGE 1). כפופה לאימות §8. |

**אין בקוד מחרוזת שאינה מופיעה בטבלאות שלהלן.**

---

## 1. הפרות שער שנמצאו ב-V2.1 — ותוקנו

זהו החלק הקריטי. שלוש מחרוזות ב-V2.1 מפרות את השער המאושר:

### 1.1 `REWRITTEN` — שורת הפרסונה

| | |
|---|---|
| **V2.1** | "לעסק של אדם אחד עד שלושה" |
| **ההפרה** | `pre-copy-gate-v1` **§12**: *"אסור לצמצם ל: 'פרילנסר' · 'עסק של אדם אחד'"* |
| **למה זה חשוב** | תקרת-ראש מספרית פוסלת מראש את הפרסונה המוגדרת — "עסק עם פעילות (גם אם יש עובדים/ספקים/לקוחות)". |
| **הנוסח החדש** | **"לבעל עסק שמנהל את היום־יום בעצמו"** |
| **נימוק** | זו לשון §12 עצמה ("בעל-עסק שמחזיק את היום־יום"). ממקדת בהתנהגות, לא בגודל. |

### 1.2 `REWRITTEN` — כרטיס הראיה בקיפול

| | |
|---|---|
| **V2.1** | כותרת **"בזמן שעבדת"** · פריטים: `קבלה מספק — נקלטה, זוהתה ותויקה` · **`חשבונית — נשלחה ונרשמה`** · `פנייה בוואטסאפ — נפתחה וממתינה לך` |
| **ההפרה** | §8 **FORBIDDEN**: *"שולחת-לבד"*. §14.6 חוק-החוקה: אין הבטחת-מה-שאין. "בזמן שעבדת … **נשלחה**" = טענת פעולה אוטונומית ברקע. Dubiz אינה שולחת דבר ללא אישור (§10.3). |
| **למה זה חשוב** | זו ההפרה החמורה ביותר ב-V2.1: היא סותרת ישירות את סקשן הבטיחות שמופיע 3 מסכים אחר כך ("שום דבר לא יוצא בלעדיך"). הדף היה סותר את עצמו. |
| **הנוסח החדש** | כותרת **"מה כבר מסודר"** (`RATIFIED` — `/home` S1) · פריטים: `קבלה מספק — ספק, סכום ותאריך זוהו` · `חשבונית פתוחה — ₪3,200 · קישור לתשלום מוכן` · `פנייה בוואטסאפ — ממתינה לתשובה שלך` |
| **נימוק** | עובר מ**פעולה** ל**מצב** — בדיוק הפרימיטיב של `visual-design-spec-v1` §14 ("מצב → למה חשוב → החלטה שלך"). "קישור לתשלום **מוכן**" (ולא "נשלח") שומר את הבעלים כיוזם. |

### 1.3 `REWRITTEN` — טענת aging ב-Before→After

| | |
|---|---|
| **V2.1** | "פתוח · 3,400 ₪ · **באיחור 12 יום**" |
| **ההפרה** | §8 **FORBIDDEN**: *"עוקבת"* מותר רק כ**הצגת סטטוס, לא aging**. §14 בספק הויזואלי: *money state … **לא** 'מאחר X ימים'*. |
| **הנוסח החדש** | "פתוח · ₪3,400 · קישור לתשלום מוכן" |
| **נימוק** | סטטוס תשלום = A (`collection` + payment links). ספירת ימי-איחור = לא נתמך. |

### 1.4 `REMOVED` — התחייבות מסחרית

| | |
|---|---|
| **V2.1** | "מייצאים הכול ויוצאים, **בלי תשלום, בלי תנאים, בלי לבקש רשות**" |
| **הבעיה** | זו **התחייבות מסחרית**, לא יכולת-מוצר. §13: אין claim מסחרי לא-מאומת. אין החלטת בעלים כתובה. |
| **הפעולה** | הוחלף בנוסח המאושר: **"הנתונים שלך נשארים שלך, עם ייצוא מסודר בכל רגע."** (`RATIFIED`, copy-v1 §8) |

### 1.5 `REMOVED` — שורת ה-Ask כשההרשמה סגורה

| | |
|---|---|
| **V2.1** | ה-Ask מציג תמיד "הדבר הראשון לוקח שתי דקות" + הצעד "נרשמים" |
| **הבעיה** | כש-`PUBLIC_SIGNUP_ENABLED=false` אין "נרשמים". V2.1 החליף רק את **תווית** ה-CTA, אך הותיר כותרת וצעדים שמתארים הרשמה. |
| **הפעולה** | הכותרת, הצעדים וטקסט-המשנה **כולם** נגזרים מהדגל. ראו §4. |

---

## 2. CHANGE 1 — משפט ה-Product Orientation (`NEW`)

**הדרישה:** רצף `moment → short product orientation → framing/support`. משפט **אחד**.

> ### "Dubiz מרכזת במקום אחד את החשבוניות, התשלומים והפניות של העסק — ומסדרת אותם."

**אימות מול `pre-copy-gate-v1` §8 (Claim Vocabulary Lock):**

| מילה | סיווג §8 | הערה |
|---|---|---|
| **מרכזת** | ✅ SAFE (רשימה מפורשת) | הפועל המוביל |
| **מסדרת** | ✅ SAFE (רשימה מפורשת) | הפועל הסוגר |
| "חשבוניות" | A | `app/api/billing/documents/*` |
| "תשלומים" | A | `app/api/payments/requests/*` |
| "פניות" | A | leads / conversations |

**אימות מול §13 (Claim Exclusions) — כל אחד נבדק ונמצא נעדר:**
`בדיוק-בזמן` ✗ · `התראות` ✗ · `מסביב-לשעון` ✗ · `scheduler/push` ✗ · `aging` ✗ · `שולחת-לבד` ✗ · `sync/מסנכרן` ✗ · `multi-user/צוות` ✗ · `AI` ✗ · `מערכת הפעלה` ✗ · `פלטפורמה` ✗ · `all-in-one` ✗ · `free/trial/מחיר` ✗ · `הוכחה חברתית` ✗ · `אינטגרציות` ✗.

**אימות מול §14 (Final Copy Guardrails):** ללא דמות ("מזכירה") ✅ · price-neutral ✅ · ללא דחיפות/פחד ✅ · לא מצמצם פרסונה ✅.

**למה לא נפגע המשפט הראשון:** ה-H1 ("הידיים באמצע עבודה, והטלפון מצלצל.") נשאר ה-beat הראשון והגדול ביותר. משפט ה-orientation נכנס **אחריו**, בגודל קטן יותר, כשורת-הסבר — ולא כ-H1 מתחרה.

---

## 3. קופי מלא — Final Candidate, סקשן אחר סקשן

### S0 · שורת זהות — `RATIFIED`
| מחרוזת | מקור |
|---|---|
| "**Dubiz** — מופעל על ידי PRO MAX GROUP" | `/home` (#246). **אילוץ compliance** — חייב להופיע כטקסט גלוי, תואם לשם באישור OAuth. |

> ⚠️ **רגרסיה ב-V2.1:** V2.1 **השמיט** את השורה הזו לגמרי. היא מוחזרת ב-Candidate.

### S1 · FOLD
| # | מחרוזת | סטטוס |
|---|---|---|
| 1 | "לבעל עסק שמנהל את היום־יום בעצמו" | `REWRITTEN` §1.1 |
| 2 | **H1:** "הידיים באמצע עבודה, והטלפון מצלצל." | `PROMOTED` — עובר §8/§13 |
| 3 | "Dubiz מרכזת במקום אחד את החשבוניות, התשלומים והפניות של העסק — ומסדרת אותם." | `NEW` §2 |
| 4 | "זה לא חוסר סדר. פשוט אין לך משרד — ויש לך משרד לנהל." | `PROMOTED` — מצב, לא יכולת |
| 5 | "עובדת על מה שכבר יש לך — מהוואטסאפ, מהמייל ומהמסמכים. בלי הקמה." | `RATIFIED` — copy-v1 שלב 1 |
| 6 | כרטיס "מה כבר מסודר" + 3 פריטים | `REWRITTEN` §1.2 |
| 7 | CTA | ראו §4 |

> **המייל:** "מהמייל" הוא נוסח **מאושר** (copy-v1 שלב 1). Gmail מסווג **B** (§9 — gated על אימות Google), ולכן הוא מופיע **רך ומשני בתוך רשימה**, ולעולם לא כמוביל ולא כהבטחה. אין שינוי מהמאושר.

### S2 · MIRROR
| מחרוזת | סטטוס |
|---|---|
| "וזה לא רגע אחד ביום." | `PROMOTED` |
| "לקוח שאל מחיר. אמרת שתחזור אליו. לא חזרת." | `PROMOTED` |
| "קבלה מספק נשארה ברכב." | `PROMOTED` |
| "מישהו לא שילם, ואתה לא זוכר מי." | `PROMOTED` |
| "יש תשלום ב-10 בחודש שאסור לפספס." | `PROMOTED` |
| "כל אחד מהם קטן. ביחד הם תפקיד מלא — ואתה עושה אותו בערב, אחרי יום עבודה שלם." | `PROMOTED` |
| "אף אחד לא ביקש ממך להיות שני אנשים." | `PROMOTED` |
| "והתפקיד הזה לא צריך להיעלם. הוא רק צריך שמישהו אחר יעשה אותו." | `PROMOTED` — ה-TURN |

> כל השורות מתארות את **עולם המשתמש**, לא את המוצר. אין בהן claim. ✅

### S3 · MECHANISM
| מחרוזת | סטטוס |
|---|---|
| "זה מתחיל מהמקום שבו העסק שלך כבר נמצא" | `PROMOTED` |
| "לא מתחילים מהזנה של לקוחות ופריטים. מחברים את מה שכבר קיים — וזה מתחיל לעבוד." | `PROMOTED` |
| "וואטסאפ" · "מייל" · "צילום מסמך" | `PROMOTED` — שמות ערוצים, §9 |
| "מסודר, בלי שהזנת כלום" | `PROMOTED` |

### S4 · SAFETY
| מחרוזת | סטטוס |
|---|---|
| "שום דבר לא יוצא בלעדיך" | `PROMOTED` |
| "אתה בוחר מה מתחבר / ואפשר לנתק בכל רגע" | `RATIFIED` §10.2/§10.4 — `gmail/disconnect`, `whatsapp/connection/disconnect` |
| "נשמר מה שקשור לעסק / חשבוניות, תשלומים, פניות של לקוחות" | `RATIFIED` §10.1 |
| "כלום לא נשלח בשמך / לא ללקוח, לא לספק, לא לרשות — בלי שאישרת" | `RATIFIED` §10.3 |
| "המידע שלך נשאר שלך / ייצוא מסודר בכל רגע" | `RATIFIED` — החליף את §1.4 |

### S5 · BEFORE → AFTER (מיזוג — CHANGE 4)
**מוזגו:** "מה שיש לך היום — ומה Dubiz עושה איתו" (V2.1 S5) + "השבוע שלך, בעוד חודש" (V2.1 S7). שניהם סיפרו before/after.

| מחרוזת | סטטוס |
|---|---|
| **כותרת:** "מה שיש לך היום — ומה Dubiz עושה איתו" | `PROMOTED` |
| `had` 1: "חשבונית ששלחת לפני חודש ולא זוכר אם שולמה" / "בין המיילים, בוואטסאפ, ובראש" | `PROMOTED` |
| `did` 1: "פתוח · ₪3,400 · קישור לתשלום מוכן" / "מופיע ברשימה אחת עם כל מה שעוד פתוח" | `REWRITTEN` §1.3 |
| `had` 2: "הודעה מלקוח שנקברה מתחת ל-40 הודעות" / "נשלחה ב-9:15, כשהידיים היו תפוסות" | `PROMOTED` |
| `did` 2: "פנייה פתוחה · ממתינה לתשובה" / "עולה למעלה עד שתטפל, ולא נעלמת" | `PROMOTED` |
| `had` 3: "קבלה מקומטת מספק, מצולמת בטלפון" / "אחת מתוך ערימה בתא הכפפות" | `PROMOTED` |
| `did` 3: "ספק · סכום · תאריך — מזוהים ומתויקים" / "בסוף החודש כבר ערוך לרואה החשבון" | `PROMOTED` |
| **שורת רוחב:** "הלקוחות · הכסף · המסמכים · השיחות — כולם סביב אותו עסק, במקום אחד." | `RATIFIED` — copy-v1 שלב 7 |
| **שורת נחיתה:** "אתה מפסיק להחזיק את זה בראש." | `PROMOTED` — מ-V2.1 S7 |

> שורת הרוחב היא **היורשת היחידה** של סקשן ה-projection. §1 של הבריף: *reveal breadth without displaying the catalog*.

### S6 · PRODUCT PROOF (נבנה מחדש — CHANGE 3)
| מחרוזת | סטטוס |
|---|---|
| **כותרת:** "זה לא מצגת. זה כבר עובד." | `RATIFIED` — copy-v1 שלב 5 |
| "אלה מסכים אמיתיים מ-Dubiz — לא חזון ולא רשימת המתנה." | `RATIFIED` — copy-v1 שלב 5 |
| תווית טאב "גבייה" → caption: "רואים מה שולם ומה עדיין פתוח, ושולחים ללקוח קישור לתשלום בלחיצה." | `RATIFIED` — copy-v1 §8 proof captions |
| תווית טאב "מסמכים" → caption: "מצלמים חשבונית — Dubiz מזהה ספק, סכום ותאריך לבד." | `RATIFIED` — copy-v1 §8 |
| תווית טאב "חשבוניות" → caption: "מפיקים חשבונית מס, ומאותו מסך רואים מה פתוח ושולחים אותה לתשלום." | `NEW` (V2 · החלטת בעלים: הוכחה = חשבונית מס שהופקה) — A: `billing/documents/[id]/issue` + `CollectionsSection` ("יתרה פתוחה" / "שלח לתשלום", מוצג רק ל-TAX_INVOICE·ISSUED). §8: "שולחת קישור-תשלום (יזום ע"י הבעלים)" = SAFE; הנושא של "שולחים" הוא הבעלים. |
| תווית טאב "לידים" → caption: "Dubiz מסדרת את הפניות לפי מי שצריך לחזור אליו קודם — ומסמנים שטופל בלחיצה." | `NEW` (V2 · החלטת בעלים: לידים מחליף את כרטיס הלקוח) — A: `/leads` W1/W2 — סדר לפי מעקב שעבר / מעקב להיום / ליד חדש (`lead-attention.ts`), פעולות "טופל" / "דחה" בשורה. §8: "מסדרת" = SAFE. **לא** "מזכירה"/"מתריעה" (FORBIDDEN) — הסדר נגזר ממועד-מעקב שהבעלים קבע. |
| ~~תווית טאב "לקוחות"~~ | **הוסר** (V2) — כרטיס לקוח = רשומה סטטית, לא עבודה. ה-caption המאושר נשאר ב-copy-v1 לשימוש עתידי. |

**V2 — נכסי הוכחה חדשים (החלטת בעלים, 2026-09-21).** ארבעת הנכסים הקודמים (`public/landing/*.webp`, 780×1688) צולמו 2026-08-02/03, **לפני** Dubiz Mist (#311) ו-interaction layer (#317) — מבנה ותוכן עדכניים, פלטה ישנה. הוחלפו ב-`public/landing/proof/{collection,documents,billing,leads}.webp`, מצולמים מחדש מה-UI הנוכחי ב-390 CSS × DPR 3 ע"י `scripts/qa/ui/homepage-proof-capture.mjs` (אפליקציה אמיתית, נתונים סינתטיים ב-network layer, ללא DB).
- **מסמכים:** החיתוך מסתיים **לפני** מחיצת "או ייבאו אוטומטית" — **Gmail אינו בפריים** (אימות Google טרם נסגר). מוצגים: דופק החודש + קליטה ידנית (העלאה / צילום).
- **חשבוניות:** חשבונית מס **שהופקה**, ללא בקשת תשלום → "יתרה פתוחה" (מציג את סכום המסמך) נכון מילולית. **אין מספר עוסק בפריים** — פרופיל הדמו ללא ח.פ., והחיתוך נעצר לפני "פרטים נוספים".
- **לידים:** `intelligence: null` בכל השורות — אין טמפרטורה / "ממתין X דק׳" (תלוי `CONVERSATION_STATE_WRITER_ENABLED`, לא מאומת). מקור "וואטסאפ" לא מוצג.
- הנכסים הישנים **לא נמחקו** (`/home` בייצור עדיין משתמש בהם).

**החלטת נכסים — `conversations.webp` אינו בשימוש.** הנכס צולם **לפני** מיגרציית Dubiz Mist (#311): רקע לבן/תכלכל, pills ציאן, אווטרים כחולים. הוא אינו תואם לפלטה הנוכחית, ושילובו לצד שלושה מסכים חמים היה קורא כמוצר אחר. §11 בספק הויזואלי: ראיית-מוצר חייבת לחזק אמון, לא לערער אותו. **פעולת המשך (מחוץ ל-scope):** צילום מחדש של מסך השיחות אחרי Mist, ואז החזרת "שיחות" כאזור הוכחה חמישי.

> שורת הרוחב (S5) ממשיכה לכלול "השיחות" — זו טענת **יכולת** מאומתת (`api/integrations/whatsapp/*`), ואינה תלויה בקיום צילום מסך עדכני.

### S7 · LEGITIMACY (CHANGE 5) — `RATIFIED` במלואו
| מחרוזת | מקור |
|---|---|
| "מסודר מול רשות המסים — ומסודר לרואה החשבון שלך." | copy-v1 שלב 6, verbatim |
| "החשבוניות והמסמכים יוצאים בפורמט תקין ומסודר. Dubiz היא תוכנה רשומה ברשות המסים (270901), והכול מוכן להעברה לרואה החשבון — בלי שתצטרך להבין בזה." | copy-v1 שלב 6, verbatim |

> **270901 מופיע פעם אחת בלבד בכל העמוד** — כאן. הוסר מה-eyebrow של הקיפול (V2.1 הציג אותו ליד ה-CTA **וגם** בפס הלגיטימציה = decoration חוזר). §11 בספק: *"270901 = trust cue, לא Hero promise"*, ו**ללא הרחבת משמעות** של הרישום (R44).

### S8 · THE ASK — תלוי-דגל, ראו §4

### S9 · FAQ — `RATIFIED` במלואו (CHANGE 6)
חמש השאלות מתועתקות verbatim מ-`copy-v1` §10 / `/home` S9. V2.1 קיצץ לשלוש **וכתב אותן מחדש** כ-PLACEHOLDER; הקיצוץ מבוטל והנוסח המאושר חוזר.

1. "כבר יש לי חשבונית ירוקה / תוכנת חשבוניות."
2. **"זה עובד עם צוות / עובדים?"** → "כרגע Dubiz בנויה לבעל העסק שמנהל את היום־יום בעצמו. גישת-צוות עם הרשאות עדיין לא קיימת — אנחנו בונים אותה."
3. "מה עם רואה החשבון שלי?"
4. "מה קורה עם הנתונים שלי?"
5. "אני חייב להעביר הכול?"

> שאלה **2** היא נכס-אמון: היא מצהירה על מה ש**אינו** קיים. §12 בבריף: *שמור במיוחד ניסוחים כנים לגבי דברים שעדיין אינם קיימים*. **אסור להסיר.** מוצגת במלואה גם במובייל.

---

## 4. CTA — התנהגות תלוית-דגל (נגזרת, לא hard-coded)

מקור האמת היחיד: **`isPublicSignupEnabled()`** (`lib/auth/signup-gate.ts`). אין דגל שני, אין כפילות לוגיקה, אין קופי קשיח.

| | `PUBLIC_SIGNUP_ENABLED=true` | לא-"true" (המצב הנוכחי) |
|---|---|---|
| **יעד CTA** | `/register` | `/login` |
| **תווית CTA** | "התחילו עכשיו" (`RATIFIED`, copy-v1 §14) | "כניסה למשתמשים קיימים" (`RATIFIED` — `app/register/page.tsx`, מסך ההרשמה הסגורה) |
| **כותרת ה-Ask** | "הדבר הראשון לוקח כמה דקות." (`PROMOTED`; מיושר ל-"מתחילים בכמה דקות" המאושר) | "ההרשמה סגורה כרגע" (`RATIFIED` — `SIGNUP_DISABLED_TITLE_HE`) |
| **גוף ה-Ask** | 3 צעדים: "נרשמים" · "מחברים מקור אחד — או מצלמים מסמך" · "רואים מה Dubiz עשתה איתו" (`PROMOTED`) | `SIGNUP_DISABLED_MESSAGE_HE` (`RATIFIED` — מיובא מהקוד, לא משוכפל) |
| **קישור שקט אחרי ההוכחה** | "לראות את זה על העסק שלך" → `/register` | **לא מרונדר** |
| **שורת סיום** | "הנתונים שלך נשארים שלך, עם ייצוא מסודר בכל רגע." | זהה |

**חוק-הברזל:** אין מסלול מהעמוד אל `/register` כאשר הדגל סגור. נאכף ב-QA אוטומטי.

**היררכיה:** בדיוק **שני** `.dz-btn-primary` בעמוד — קיפול + Ask. ה-login ב-header יורד ל-**ghost** ואינו teal מלא.

---

## 5. מה שלא נכנס — במכוון

| נושא | החלטה |
|---|---|
| **מחיר** | אין סקשן, אין slot, אין "בקרוב". §13 — price-neutral עד הכרעת-מחיר נפרדת. |
| **נוכחות אנושית** | אין mascot / avatar / stock photo / testimonial. הוראת בעלים מפורשת. |
| **הוכחה חברתית** | אין. §11 = `C — לא להמציא`. |
| **מספרי הקצאה** | `[REQUIRES VERIFICATION]` — לא נשלח. |
| **טיוטות בוט** | ⚠️ **caption הבוט של `/home` לא הועתק ל-Candidate.** `BOT_LLM_DRAFTS_ENABLED` הוא fail-closed ודורש `OPENAI_API_KEY`. עד אימות מצב הדגל בייצור, אין טענת בוט בעמוד. |

---

## 6. מטריצת אימות — כל claim והעוגן שלו

| Claim בעמוד | דירוג | עוגן firsthand |
|---|---|---|
| ריכוז חשבוניות/תשלומים/פניות | A | `api/billing/documents`, `api/payments/requests`, leads |
| OCR — ספק/סכום/תאריך | A | documents pipeline |
| קליטה מוואטסאפ | A | `api/integrations/whatsapp/*` |
| קליטה מהמייל | **B** | `api/integrations/gmail/*` — gated; נוסח רך ומשני בלבד |
| סטטוס תשלום + קישור לתשלום | A | `api/payments/requests`, collection workspace |
| כרטיס לקוח מרוכז | A | `customers/[id]` → CustomerCard |
| חשבונית / הצעת מחיר / טיוטה | A | `api/billing/documents/[id]/{issue,convert-to-invoice}` |
| ייצוא מסודר לרו"ח | A | `api/reports/uniform` (מבנה אחיד, #145) |
| תוכנה רשומה 270901 | A | תעודת רישום — **רישום בלבד** |
| ניתוק חיבור ביוזמת הבעלים | A | `gmail/disconnect`, `whatsapp/connection/disconnect` |
| "צוות עדיין לא קיים" | A (כנות) | אין `BusinessMembership` |
| self-serve | A / **תלוי-דגל** | `/register` + `signup-gate` |

---

## 7. סטטוס

> **המסמך מחייב את `/home-candidate`.**
> `/home` (ייצור) ו-`/home-prototype` (V2.1) **אינם משתנים** ונשארים כפי שהם להשוואה.
> כל שינוי קופי עתידי בעמוד המועמד — **כאן קודם, ואז בקוד**.

*אינו מחליף את `copy-v1.md` ואינו מרחיב את `pre-copy-gate-v1.md`. במקרה של סתירה — מסמכי v1 מנצחים.*
